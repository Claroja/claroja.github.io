import{_ as e}from"./plugin-vue_export-helper-x3n3nnut.js";import{o as a,c as d,e as s}from"./app-XqA98pG8.js";const n={},o=s(`<h1 id="shotcut" tabindex="-1"><a class="header-anchor" href="#shotcut" aria-hidden="true">#</a> shotcut</h1><h2 id="创建快捷方式" tabindex="-1"><a class="header-anchor" href="#创建快捷方式" aria-hidden="true">#</a> 创建快捷方式</h2><p>比如<code>root</code>用户在<code>/root/</code>目录下, 下载了<code>node-v14.15.1-linux-x64</code>, 可以将运行程序软连接到<code>/usr/local/bin/</code>下.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ln</span> <span class="token parameter variable">-s</span> /root/node-v14.15.1-linux-x64/bin/node /usr/local/bin/node
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>不需要时直接删除<code>/usr/local/bin/node</code>软连接即可.</p><h2 id="目录" tabindex="-1"><a class="header-anchor" href="#目录" aria-hidden="true">#</a> 目录</h2><p>bin是binary的缩写，二进制。 sbin意义为system binary</p><table><thead><tr><th>目录</th><th>描述</th></tr></thead><tbody><tr><td><code>/bin</code></td><td>常是普通用户和超级用户都会用到的必要的命令，例如<code>ls</code>，<code>pwd</code></td></tr><tr><td><code>/sbin</code></td><td>通常是系统管理员使用的必要的来管理系统的命令，例如<code>shutdown</code>，<code>ifconfig</code></td></tr><tr><td><code>/usr/bin</code></td><td>通常是一些非必要的，但是普通用户和超级用户都可能使用到的命令，例如gcc，ldd</td></tr><tr><td><code>/usr/sbin</code></td><td>通常是一些非必要的，由系统管理员来使用的管理系统的命令，例如crond，httpd</td></tr><tr><td><code>/usr/local/bin</code></td><td>通常是用户后来安装的软件，可能被普通用户或超级用户使用。</td></tr><tr><td><code>/usr/local/sbin</code></td><td>通常是用户后来安装的软件，一般是用来管理系统的，被系统管理员使用。</td></tr></tbody></table><h2 id="或者直接添加环境变量" tabindex="-1"><a class="header-anchor" href="#或者直接添加环境变量" aria-hidden="true">#</a> 或者直接添加环境变量</h2><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">export</span> 变量名<span class="token operator">=</span><span class="token string">&#39;值&#39;</span>
<span class="token builtin class-name">export</span> <span class="token assign-left variable"><span class="token environment constant">PATH</span></span><span class="token operator">=</span><span class="token environment constant">$PATH</span><span class="token builtin class-name">:</span><span class="token environment constant">$HOME</span>/bin:<span class="token variable">$ORACLE_HOME</span>/bin
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="系统环境变量" tabindex="-1"><a class="header-anchor" href="#系统环境变量" aria-hidden="true">#</a> 系统环境变量</h3><p>系统环境变量对全部的用户生效，设置系统环境变量有三种方法。</p><ol><li>系统环境变量<code>/etc/profile</code>, 用户登录时<code>/etc/profile</code>文件. 或者手动执行<code>source /etc/profile</code>. 不推荐.</li><li>在<code>/etc/profile.d</code>目录中增加环境变量脚本文件，这是Linux推荐的方法。<code>/etc/profile</code>在每次启动时会执行<code>/etc/profile.d</code>下全部的脚本文件。</li><li>在<code>/etc/bashrc</code>文件中设置环境变量。用户使用的<code>bash shell</code>时调用, 不建议.</li></ol><h3 id="用户环境变量" tabindex="-1"><a class="header-anchor" href="#用户环境变量" aria-hidden="true">#</a> 用户环境变量</h3><p>用户环境变量只对当前用户生效，设置用户环境变量也有多种方法。</p><ol><li><code>.bash_profile</code>（推荐首选）当用户登录时执行</li><li><code>.bashrc</code> 每次打开终端时执行</li><li><code>.bash_logout</code> 退出终端时执行</li><li><code>.bash_history</code> 保存了当前用户使用过的历史命令</li></ol><h3 id="环境变量脚本执行顺序" tabindex="-1"><a class="header-anchor" href="#环境变量脚本执行顺序" aria-hidden="true">#</a> 环境变量脚本执行顺序</h3><p><code>/etc/profile-&gt;/etc/profile.d-&gt;/etc/bashrc-&gt;用户的.bash_profile-&gt;用户的.bashrc</code></p><h2 id="path变量" tabindex="-1"><a class="header-anchor" href="#path变量" aria-hidden="true">#</a> PATH变量</h2><p>可执行程序的搜索目录，可执行程序包括Linux系统命令和用户的应用程序。PATH环境变量存放的是目录列表，目录之间用冒号:分隔，最后的圆点.表示当前目录。PATH缺省包含了Linux系统命令所在的目录（/usr/local/bin:/usr/bin:/usr/local/sbin:/usr/sbin）</p>`,20),t=[o];function c(i,r){return a(),d("div",null,t)}const p=e(n,[["render",c],["__file","linux_shotcut.html.vue"]]);export{p as default};
